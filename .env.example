# ========== DOCKER BUILD CONFIGURATION ==========
# Build target: production or development
# production: Runs main.py for continuous background job syncing
# development: Keeps container running for manual testing/debugging
BUILD_TARGET=production

# ========== FTP CONFIGURATION ==========
# FTP server hostname or IP address
FTP_HOST=ftp.example.com

# FTP username for authentication
FTP_USERNAME=ftp_user

# FTP password for authentication
FTP_PASSWORD=your_secure_password

# Target directory on FTP server where job data will be uploaded
# The crawler will create jobs/ subdirectory for individual job descriptions
FTP_DIRECTORY=/public_html/jobs

# ========== SCHEDULER CONFIGURATION ==========
# Hours between job synchronization runs
# Example: 24 = run once per day, 6 = run every 6 hours
HOURS_DELAY=24

# ========== SERVICE CONFIGURATION ==========
# Windows service name (used for non-Docker deployments)
SERVICE_NAME=JobViteCrowler

# Windows service description (used for non-Docker deployments)
SERVICE_DESCRIPTION=Lean Tech Job Vitae Web Crawler Service
